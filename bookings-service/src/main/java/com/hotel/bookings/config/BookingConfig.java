package com.hotel.bookings.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BookingConfig {
    // RestTemplate bean is already provided by common module
}
